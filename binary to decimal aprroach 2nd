// Online C++ compiler to run C++ program online
#include <iostream>
#include<math.h>
using namespace std;


int main() {
int num=101,total=0;
int i=0;
while(num>0)
{
 if(num%10==1)
 {
     total+=pow(2,i);
 }
 num/=10;
 i++;
}
cout<<total;
    return 0;
}
