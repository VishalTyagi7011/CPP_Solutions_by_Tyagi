// Online C++ compiler to run C++ program online
#include <iostream>
#include<algorithm>


int main() {
int number=11;
std::string str="";
while(number!=0)
{
    str+=std::to_string(number%2);
    number/=2;
}
reverse(str.begin(),str.end());
int x=stoi(str);
std::cout<<x;
    return 0;
}
